---
galaxy_info:
  author: Claus Koch
  description: Role for PgBouncer
  company: Affinitas GmbH
  min_ansible_version: 2.0.0
  license: LGPL
  platforms:
    - name: Debian
      versions:
        - jessie
  categories:
    - database
    - postgresql

# dependencies:
#   - { role: 'debops.core', when: ansible_os_family == 'Debian' }

